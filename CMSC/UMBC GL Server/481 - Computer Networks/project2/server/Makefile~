CC=gcc
CFLAGS = -g -O2 -D_REENTRANT -Wall
OBJ = error.o wrapsocket.o wrapio.o
DEPS = common.h

all: myftp ftpsvr

myftp:	$(OBJ) ftpcli.o
	$(CC) $(CFLAGS) -o $@ $^

ftpsvr:	$(OBJ) ftpsvr.o
	$(CC) $(CFLAGS) -o $@ $^

%.o: %.c $(DEPS)
	$(CC) $(CFLAGS) -o $@ -c $<

clean:
	rm -rf *.o myftp ftpsvr